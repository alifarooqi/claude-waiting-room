---
"@waiting-room/exercise-math": patch
---

smoke-test the release pipeline: bump exercise-math to 1.0.1. (Will also widen its declared `@waiting-room/sdk` range to `^1.0.1` via `updateInternalDependencies: "patch"`.)
