---
"@waiting-room/sdk": patch
---

test the npm release workflow end-to-end. This is a no-op bump of the SDK to confirm the release-npm.yml pipeline ships correctly: changesets version + commit, sequential publish, NPM_TOKEN authentication.
