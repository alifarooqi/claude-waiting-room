# Dependencies
node_modules/
.pnpm-store/

# Build artifacts
packages/*/dist/
daemon/bin/
*.tsbuildinfo
coverage/

# Packed platform binaries (built via make cli-pack / plugin-pack)
packages/plugin-claude/bin/waiting-room-*
packages/cli-darwin-*/bin/
packages/cli-linux-*/bin/

# Plugin-claude install cache (populated on first launch by the resolver;
# runtime artifact, never tracked)
packages/plugin-claude/bin/.wr-cache/

# Waiting Room runtime
*.sock
.waiting-room/

# Reference docs (local only, not part of the published repo)
/resources/

# OS / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
