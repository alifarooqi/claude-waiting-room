{
  "name": "@waiting-room/cli",
  "version": "1.0.0",
  "description": "Binary distribution wrapper for the Waiting Room daemon (waiting-room).",
  "license": "MIT",
  "type": "module",
  "bin": {
    "waiting-room": "./bin/cli.js"
  },
  "files": [
    "bin",
    "README.md"
  ],
  "scripts": {
    "build": "echo \"@waiting-room/cli: platform binaries resolved at install (make cli-pack)\"",
    "lint": "node --check bin/cli.js",
    "test": "echo \"no tests yet\"",
    "clean": "echo ok"
  },
  "optionalDependencies": {
    "@waiting-room/cli-darwin-arm64": "workspace:*",
    "@waiting-room/cli-darwin-amd64": "workspace:*",
    "@waiting-room/cli-linux-arm64": "workspace:*",
    "@waiting-room/cli-linux-amd64": "workspace:*"
  }
}
