{
  "name": "@waiting-room/exercise-math",
  "version": "0.1.0",
  "private": true,
  "description": "Multiple-choice math quiz — a Waiting Room reference activity.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"no tests yet\"",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@waiting-room/tsconfig": "workspace:*",
    "typescript": "^5.6.0"
  }
}
