{
  "name": "@waiting-room/plugin-claude",
  "version": "1.0.0",
  "description": "Claude Code plugin that pauses side activities and snaps tmux focus back when Claude needs you. Daemon bootstraps on SessionStart, then /waiting-room:start launches games.",
  "license": "MIT",
  "type": "module",
  "files": [
    "hooks",
    ".claude-plugin",
    "skills",
    "bin",
    "README.md"
  ],
  "scripts": {
    "build": "node -e \"console.log('plugin-claude: static manifest, nothing to compile')\"",
    "lint": "node -e \"require('fs').accessSync('hooks/hooks.json'); require('fs').accessSync('skills/start/SKILL.md'); require('fs').accessSync('bin/waiting-room')\"",
    "test": "echo \"no tests yet\"",
    "clean": "echo ok"
  }
}
